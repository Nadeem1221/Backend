//require( ) se sab aa jata hai lekin import me jo hum chahate he bo ata hai

import { sum ,PI} from "./math1.js";  

console.log(sum(1,2));

/*
loading is synchronous for 'require()'
but can be asynchronous for 'import()'

we can also import packages
*/ 
